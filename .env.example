# Frontend environment — copy to .env in project root
VITE_API_URL=http://127.0.0.1:8000/api

# OAuth credentials are configured in larte-backend/.env (see larte-backend/docs/OAUTH_SETUP.md).
# Social login availability is reported by GET /api/auth/providers.
